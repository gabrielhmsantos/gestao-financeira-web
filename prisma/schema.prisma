// Prisma schema — M0 bootstrap (sem entidades de domínio).
// User / Despesa entram em M1+.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
